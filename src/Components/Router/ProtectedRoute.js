import PropTypes from "prop-types";

const ProtectedLoginComponent =  ({render})=>{

}

ProtectedLoginComponent.PropTypes = {
    render: PropTypes.element.isRequired
}

export default ProtectedLoginComponent;